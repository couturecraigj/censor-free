import * as Types from '../../../common/types';

const OBJECT_ENUM = Types.OBJECT_TYPE_MAP;

const FILTER_ENUM = Types.FILTER_TYPE_MAP;

const POSTNODE_ENUM = Types.POST_TYPE_MAP;

export default { OBJECT_ENUM, FILTER_ENUM, POSTNODE_ENUM };
